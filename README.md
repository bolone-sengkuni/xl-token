# 🧩 XL Token Generator

Script sederhana untuk mendapatkan token XL secara otomatis.

## 🚀 Cara Penggunaan

Jalankan perintah berikut di terminal kamu:

```bash
git clone https://github.com/bolone-sengkuni/xl-token && cd xl-token && chmod +x ./xl-token && ./xl-token
